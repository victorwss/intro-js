/**
 * Recebe um vetor de números e retorna o menor elemento do vetor. 
 * Se o vetor estiver vazio, retorna undefined.
 * @param {Array} vetor 
 * @return {Number} o menor valor.
 */
function acharMenor(vetor) { }

/**
 * Recebe um vetor de números e devolve um outro vetor apenas com os números pares deste vetor.
 * Se o vetor estiver vazio, devolve um vetor vazio.
 * @param {Array} vetor com números inteiros.
 * @returns {Array} vetor contendo apenas números pares do original (ou vazio se não houver nenhum)
 */
function acharPares(vetor) { }

/**
 * Receba um vetor de notas (números não negativos apenas) e devolva um vetor resultante com as menores notas fora. 
 * As regras de exclusão são:
 *  - Se o vetor tiver até 5 notas, não retirar nada.
 *  - Se o vetor tiver mais que 5 notas, mas até 7, retirar as 2 menores notas.
 *  - Se o vetor tiver mais que 7 notas, retirar as 3 menores notas.
 * O vetor resultado deve estar na mesma ordem que o anterior, a menos das menores notas
 * Se o vetor estiver vazio, deve voltar um vetor vazio.
 * Exemplos: 
 *      [1,2,3,4,5,6,7] -> [3,4,5,6,7]
 *      [8,7,1,9,0,3,1.5,3,2] -> [8,7,9,3,3,2]
 * @param {Array} vetorDeNotas vetor contendo números não negativos.
 * @returns {Array} vetor de notas resultantes.
 */
function retiraAsMenores(vetorDeNotas) { }