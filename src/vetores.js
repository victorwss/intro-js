/**
 * Recebe um vetor de números e retorna o menor elemento do vetor. Se o vetor estiver vazio, retorna undefined.
 * @param {Array} vetor 
 * @return {Number} o menor valor.
 */
function acharMenor(vetor) {}

// Devolva um vetor com apenas pares

// Inverter uma String

// Media de numeros 